[build]
  command   = "npm run build"
  publish   = "dist/birthday-wishes/browser"

# SPA fallback — serve index.html for all routes Angular handles
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
